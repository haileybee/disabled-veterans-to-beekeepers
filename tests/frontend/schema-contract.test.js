import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs';const sql=fs.readFileSync(new URL('../../supabase/migrations/2026082902_veterans_site.sql',import.meta.url),'utf8');const owners=fs.readFileSync(new URL('../../supabase/migrations/2026083001_veterans_multiple_owners.sql',import.meta.url),'utf8');test('veterans schema stays namespaced and uses RLS',()=>{for(const table of['veterans_admins','veterans_products','veterans_orders','veterans_order_items','veterans_chat_messages','veterans_chat_blocks']){assert.match(sql,new RegExp(`create table if not exists public\\.${table}`));assert.match(sql,new RegExp(`alter table public\\.${table} enable row level security`));}});test('capture finalization is service role only and idempotent',()=>{assert.match(sql,/if v_order\.payment_status = 'COMPLETED'/);assert.match(sql,/grant execute on function public\.veterans_finalize_capture\(text,text,text,jsonb\) to service_role/);});test('multiple approved owner accounts are supported',()=>{assert.match(owners,/drop index if exists veterans_single_active_owner_idx/i);assert.match(owners,/donaldrschafer@gmail\.com/i);assert.match(owners,/haileybee913@gmail\.com/i);assert.doesNotMatch(owners,/create unique index .*single_active_owner/i);});

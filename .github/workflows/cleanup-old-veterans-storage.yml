name: Cleanup old veterans storage
on:
  push:
    branches: [ops/cleanup-old-veterans-storage]
jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Delete retired veterans storage buckets
        run: |
          set -euo pipefail
          curl -fsS "https://zljduaahbyxnuglbugar.supabase.co/functions/v1/veterans-chat-post"
